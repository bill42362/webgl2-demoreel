// {{ properCase filename }}.jsx
'use strict';
import React from 'react';
import Enzyme, { shallow } from 'enzyme';
import Adapter from 'enzyme-adapter-react-16';
import {{ properCase filename }} from '../{{ properCase filename }}.jsx';

Enzyme.configure({ adapter: new Adapter() });

describe('component {{ properCase filename }}', () => {
  const props = { };
  it('should render without crash', () => {
    const wrapper = shallow(<{{ properCase filename }} {...props} />);
  });
});
